# Только для CI и локальной разработки — в Docker-образ не попадает.
pytest>=8.3
pytest-asyncio>=1.0
pyflakes>=3.2
# линтер CI (6.21); правила — в pyproject.toml
ruff>=0.16
# ASGI-транспорт для тестов админ-панели: без сети и без реального сервера
httpx>=0.27
