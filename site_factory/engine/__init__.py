"""Движок подбора и рендера (13-шаблоны-сайтов.md §3).

Порядок вызовов — он же порядок чтения модулей:

    profile.py   признаки лида, у каждого флаг known
    render.py    seed, трек, рецепт, пресет; собирает всё и рендерит Jinja
    compose.py   роли рецепта -> секции, лестница деградации, след решения
    gates.py     отсев вариантов по requires (и язык условий контрактов)
    slots.py     JSON слотов: факты из белого списка, free — из рецепта
    score.py     3*data_fit + 2*niche_affinity + 2*novelty - perf_cost
    checks/      автопроверки готовой страницы, режутся последними
"""
